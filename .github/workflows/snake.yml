name: Generate Snake

on:
  schedule:
    - cron: "0 */2 * * *" # Atualiza a cada 2 horas
  workflow_dispatch: 
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Snake Animation
        uses: Platane/snk@v3
        with:
          github_user_name: laizaerochar
          outputs: |
            dist/github-contribution-runner-snake.svg?color_snake=#C71585&color_dots=#ebedf0,#f1f8ff,#ffb6c1,#ff69b4,#C71585
            dist/github-contribution-runner-snake-dark.svg?palette=github-dark&color_snake=#C71585&color_dots=#161b22,#4d0026,#800040,#b30059,#e60073
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push to Output Branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
